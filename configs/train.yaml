defaults:
  - data: darcy
  - model: fno
  - trainer: default
  - logger: wandb
  - callbacks: default
  - _self_

loss:
  training: l2

opt:
  learning_rate: 1e-3
  weight_decay: 1e-4
  step_size: 100
  gamma: 0.5
