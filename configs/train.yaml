defaults:
  - data: darcy
  - model: fno
  - trainer: default
  - logger: wandb
  - callbacks: default
  - loss: data_only
  - _self_ #todo.do we need this?

opt:
  learning_rate: 1e-3
  weight_decay: 1e-4
  step_size: 100
  gamma: 0.5
