model_checkpoint:
  _target_: lightning.pytorch.callbacks.ModelCheckpoint
  monitor: val_${data.test_resolutions.0}_l2/dataloader_idx_0
  mode: min
  save_top_k: 1
  save_last: true
  filename: best

model_checkpoint_val61:
  _target_: lightning.pytorch.callbacks.ModelCheckpoint
  monitor: val_${data.test_resolutions.1}_l2/dataloader_idx_1
  mode: min
  save_top_k: 1
  save_last: false
  filename: best_val61

model_checkpoint_val211:
  _target_: lightning.pytorch.callbacks.ModelCheckpoint
  monitor: val_${data.test_resolutions.2}_l2/dataloader_idx_2
  mode: min
  save_top_k: 1
  save_last: false
  filename: best_val211
