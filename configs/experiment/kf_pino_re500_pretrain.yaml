# @package _global_
# PINO pretraining on Re=500 Kolmogorov flow — sharp baseline, fixed 240/30/30 split,
# independent (uncorrelated) chains. Relies on configs/train_kf.yaml defaults for
# arch/loss/opt/trainer; only the Re-identifying fields are pinned here.

data:
  data_path: /system/user/studentwork/wehofer/data/ns/NS_fine_Re500_T128_res128_part0.npy

loss:
  re: 500

logger:
  wandb:
    name: pino-re500-pretrain
