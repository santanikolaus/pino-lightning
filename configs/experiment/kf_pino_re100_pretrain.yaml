# @package _global_
# PINO pretraining on Re=100 Kolmogorov flow — sharp baseline, fixed 240/30/30 split,
# independent (uncorrelated) chains. Relies on configs/train_kf.yaml defaults for
# arch/loss/opt/trainer; only the Re-identifying fields are pinned here.

data:
  data_path: /system/user/studentwork/wehofer/data/ns/NS_fine_Re100_T128_res128_part0.npy

loss:
  re: 100

trainer:
  check_val_every_n_epoch: 1

logger:
  wandb:
    name: pino-re100-pretrain
