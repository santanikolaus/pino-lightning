model_arch: fno
data_channels: 4       # gridx, gridy, gridt, IC vorticity
out_channels: 1        # predicted vorticity
n_modes: [8, 8, 8]     # spectral modes for x, y, t
hidden_channels: 64
n_layers: 4
lifting_channel_ratio: 2
projection_channel_ratio: 2
domain_padding: 0.0   # Fraction of each spatial dim added as zero-padding before spectral convs
                      # to suppress aliasing at the periodic boundary (paper uses ~0.09:
                      # 128-grid → ~140 after padding).  0.0 = padding off.
                      # neuralop strips the padding automatically so output shape always
                      # matches input spatial dims — no manual strip needed in kf_forward.
norm: null
fno_skip: linear
implementation: factorized
use_channel_mlp: true
channel_mlp_expansion: 0.5
channel_mlp_dropout: 0.0
separable: false
factorization: null
rank: 1.0
fixed_rank_modes: false
stabilizer: "None"
